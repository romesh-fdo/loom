<?php

use Illuminate\Support\Facades\Route;
use {{ controllerFqn }};

Route::prefix(config('loom.admin.route_prefix'))
    ->name(config('loom.admin.route_name_prefix'))
    ->group(function () {
        Route::resource('{{ route }}', {{ controllerFqn }}::class)->except(['show']);
    });
